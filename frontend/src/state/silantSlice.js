import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const silantSlice = createSlice({
    name: "silantSlice",
    initialState,
    reducers: {}
});

export const {} = silantSlice.actions;
export default silantSlice.reducer;